{%- assign s = section.settings -%}
{%- assign images = s.images -%}
{%- if images and images.size > 0 -%}
<section class="sf-section sf-insta{% if section.in_column %} sf-in-column{% endif %}">
  <div class="sf-container">
    {%- if s.heading != blank or s.handle != blank -%}
    <div class="sf-shead sf-shead-center">
      {%- if s.heading != blank -%}<span class="sf-eyebrow">{{ s.heading }}</span>{%- endif -%}
      {%- if s.handle != blank -%}<h2 class="sf-title" data-el="title">{{ s.handle }}</h2>{%- endif -%}
    </div>
    {%- endif -%}
    <div class="sf-insta-grid">
      {%- for item in images -%}{%- if item.image != blank -%}
      <a class="sf-insta-cell" href="{{ item.href | default: '#' }}" data-el-item="images:{{ forloop.index0 }}" data-el="item"><img src="{{ item.image }}" alt="Instagram post"><span class="sf-insta-ic"><i class="fab fa-instagram"></i></span></a>
      {%- endif -%}{%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
