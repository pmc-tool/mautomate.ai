{%- assign href = routes.root_url | append: '/products/' | append: product.handle -%}
{%- assign main = product.featured_image.url | default: product.featured_image -%}
{%- if main == blank and product.images -%}{%- assign main = product.images[0].url -%}{%- endif -%}
{%- if main == blank -%}{%- assign main = '/helendo/images/product/1_1-300x300.jpg' -%}{%- endif -%}
{%- assign hover = main -%}
{%- if product.images and product.images.size > 1 -%}{%- assign hover = product.images[1].url -%}{%- endif -%}
{%- assign on_sale = false -%}
{%- if product.compare_at_price and product.compare_at_price > product.price -%}{%- assign on_sale = true -%}{%- endif -%}
<article class="hl-card">
  <div class="hl-card-media">
    {%- if on_sale -%}<span class="hl-card-badge">Sale</span>{%- endif -%}
    <a href="{{ href }}" class="hl-card-img">
      <img class="hl-card-img-main" src="{{ main }}" alt="{{ product.title }}" loading="lazy">
      {%- if hover != main -%}<img class="hl-card-img-hover" src="{{ hover }}" alt="{{ product.title }}" loading="lazy">{%- endif -%}
    </a>
  </div>
  <div class="hl-card-body">
    <h6 class="hl-card-title"><a href="{{ href }}">{{ product.title }}</a></h6>
    <div class="hl-card-price">
      {%- if on_sale -%}<span class="hl-price-now">{{ product.price | money }}</span> <span class="hl-price-old">{{ product.compare_at_price | money }}</span>
      {%- else -%}<span class="hl-price-now">{{ product.price | money }}</span>{%- endif -%}
    </div>
  </div>
</article>
