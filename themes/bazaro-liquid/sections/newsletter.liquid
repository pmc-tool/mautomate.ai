{%- assign s = section.settings -%}
{%- if s.title != blank -%}
<section class="bz-section bz-newsletter">
  <div class="bz-container">
    <div class="bz-newsletter-inner">
      <span class="bz-subtitle">Newsletter</span>
      <h2 class="bz-title" data-el="heading">{{ s.title }}</h2>
      {%- if s.subtitle != blank -%}<p class="bz-newsletter-sub" data-el="text">{{ s.subtitle }}</p>{%- endif -%}
      <form class="bz-newsletter-form" data-newsletter data-el="form">
        <input type="email" placeholder="{{ s.placeholder | default: 'Enter your email address' }}" aria-label="Email address" data-el="input" required>
        <button type="submit" class="bz-newsletter-btn" data-el="button">{{ s.button | default: 'Subscribe' }}</button>
      </form>
      {%- if s.provider_note != blank -%}<p class="bz-newsletter-note">{{ s.provider_note }}</p>{%- endif -%}
    </div>
  </div>
</section>
{%- endif -%}
