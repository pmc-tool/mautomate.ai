<div class="bz-pageband">
  <div class="bz-container">
    <h1 class="bz-pageband-title">Addresses</h1>
    <nav class="bz-pageband-crumbs"><a href="{{ routes.root_url }}">Home</a><span>/</span><a href="{{ routes.account_url }}">My account</a><span>/</span><span class="bz-current">Addresses</span></nav>
  </div>
</div>
<section class="bz-section bz-account">
  <div class="bz-container">
    <div class="bz-acc-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'addresses' %}
      <div class="bz-acc-main">
        <div class="bz-acc-toolbar">
          <button type="button" class="bz-btn" data-address-new>Add address</button>
        </div>

        {%- if customer.addresses and customer.addresses.size > 0 -%}
        <div class="bz-addr-grid">
          {%- for address in customer.addresses -%}
          <div class="bz-addr-card">
            <p class="bz-addr-name">{{ address.first_name }} {{ address.last_name }}
              {%- if address.is_default_shipping %} <span class="bz-acc-badge">Default shipping</span>{% endif -%}
              {%- if address.is_default_billing %} <span class="bz-acc-badge">Default billing</span>{% endif -%}
            </p>
            <p class="bz-addr-lines">{{ address.address_1 }}{% if address.address_2 != blank %}, {{ address.address_2 }}{% endif %}<br>
            {{ address.city }}{% if address.province != blank %}, {{ address.province }}{% endif %} {{ address.postal_code }}<br>
            {{ address.country_code }}{% if address.phone != blank %} &middot; {{ address.phone }}{% endif %}</p>
            <div class="bz-addr-actions">
              <button type="button" class="bz-acc-link" data-address-edit
                data-a-address_id="{{ address.id }}"
                data-a-first_name="{{ address.first_name }}"
                data-a-last_name="{{ address.last_name }}"
                data-a-company="{{ address.company }}"
                data-a-address_1="{{ address.address_1 }}"
                data-a-address_2="{{ address.address_2 }}"
                data-a-city="{{ address.city }}"
                data-a-postal_code="{{ address.postal_code }}"
                data-a-province="{{ address.province }}"
                data-a-country_code="{{ address.country_code | downcase }}"
                data-a-phone="{{ address.phone }}">Edit</button>
              <button type="button" class="bz-acc-link bz-acc-link-danger" data-address-delete="{{ address.id }}">Delete</button>
            </div>
          </div>
          {%- endfor -%}
        </div>
        {%- else -%}
        <div class="bz-acc-empty"><p>No saved addresses yet.</p></div>
        {%- endif -%}

        <form class="bz-addr-form" data-account-form="address" data-success="Address saved." hidden>
          <h2 class="bz-acc-h2">Address details</h2>
          <input type="hidden" name="address_id" value="">
          <div class="bz-field-row">
            <label class="bz-field"><span>First name</span><input type="text" name="first_name" required></label>
            <label class="bz-field"><span>Last name</span><input type="text" name="last_name" required></label>
          </div>
          <label class="bz-field"><span>Company <em>(optional)</em></span><input type="text" name="company"></label>
          <label class="bz-field"><span>Address</span><input type="text" name="address_1" required></label>
          <label class="bz-field"><span>Apartment, suite, etc. <em>(optional)</em></span><input type="text" name="address_2"></label>
          <div class="bz-field-row">
            <label class="bz-field"><span>City</span><input type="text" name="city" required></label>
            <label class="bz-field"><span>Postal code</span><input type="text" name="postal_code" required></label>
          </div>
          <div class="bz-field-row">
            <label class="bz-field"><span>Province / state <em>(optional)</em></span><input type="text" name="province"></label>
            <label class="bz-field"><span>Country</span>
              <select class="bz-select" name="country_code" required>
                {%- for country in countries -%}
                <option value="{{ country.code }}">{{ country.name }}</option>
                {%- endfor -%}
              </select>
            </label>
          </div>
          <label class="bz-field"><span>Phone <em>(optional)</em></span><input type="tel" name="phone"></label>
          <div class="bz-field-row">
            <label class="bz-check"><input type="checkbox" name="default_shipping"><span>Default shipping address</span></label>
            <label class="bz-check"><input type="checkbox" name="default_billing"><span>Default billing address</span></label>
          </div>
          <p class="bz-form-msg" data-form-msg hidden></p>
          <button type="submit" class="bz-btn">Save address</button>
        </form>
      </div>
    </div>
  </div>
</section>
