<section class="rk-crumbband">
  <div class="rk-container">
    <h1 class="rk-crumb-title">Order {{ order.name }}</h1>
    <ul class="rk-crumb-menu">
      <li><a href="{{ routes.root_url }}">Home</a></li>
      <li><a href="{{ routes.account_url }}/orders">Orders</a></li>
      <li><span class="rk-crumb-active">{{ order.name }}</span></li>
    </ul>
  </div>
</section>
<section class="rk-section rk-account">
  <div class="rk-container">
    <div class="rk-acct-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'orders' %}
      <div class="rk-acct-main">
        <p class="rk-order-meta">Placed {{ order.created_at | date: "%B %d, %Y" }} &middot; <span class="rk-badge">{{ order.fulfillment_status | replace: "_", " " | capitalize }}</span> <span class="rk-badge">{{ order.payment_status | replace: "_", " " | capitalize }}</span></p>

        <div class="rk-order-lines">
          {%- for item in order.items -%}
          <div class="rk-order-line">
            {%- if item.image %}<img src="{{ item.image }}" alt="{{ item.title }}">{% endif -%}
            <div class="rk-order-line-info">
              <p class="rk-order-line-title">{{ item.title }}</p>
              {%- if item.variant_title != blank and item.variant_title != "Default Variant" -%}
              <span class="rk-order-line-variant">{{ item.variant_title }}</span>
              {%- endif -%}
              <span class="rk-order-line-qty">Qty {{ item.quantity }} &times; {{ item.unit_price | money }}</span>
            </div>
            <span class="rk-order-line-total">{{ item.total | money }}</span>
          </div>
          {%- endfor -%}
        </div>

        <div class="rk-order-summary">
          <h2 class="rk-cart-summary-title">Summary</h2>
          <div class="rk-cart-sum-row"><span>Subtotal</span><span>{{ order.subtotal | money }}</span></div>
          {%- if order.discount_total > 0 -%}
          <div class="rk-cart-sum-row rk-cart-discount"><span>Discount</span><span>&minus;{{ order.discount_total | money }}</span></div>
          {%- endif -%}
          <div class="rk-cart-sum-row"><span>Shipping</span><span>{{ order.shipping_total | money }}</span></div>
          {%- if order.tax_total > 0 -%}
          <div class="rk-cart-sum-row"><span>Tax</span><span>{{ order.tax_total | money }}</span></div>
          {%- endif -%}
          <div class="rk-cart-sum-row rk-cart-total"><span>Total</span><span>{{ order.total | money }}</span></div>
        </div>

        <div class="rk-order-addresses">
          {%- if order.shipping_address -%}
          <div class="rk-addr-card">
            <span class="rk-acct-card-label">Shipping address</span>
            <p class="rk-addr-lines">{{ order.shipping_address.first_name }} {{ order.shipping_address.last_name }}<br>
            {{ order.shipping_address.address_1 }}{% if order.shipping_address.address_2 != blank %}, {{ order.shipping_address.address_2 }}{% endif %}<br>
            {{ order.shipping_address.city }}{% if order.shipping_address.province != blank %}, {{ order.shipping_address.province }}{% endif %} {{ order.shipping_address.postal_code }}<br>
            {{ order.shipping_address.country_code }}</p>
          </div>
          {%- endif -%}
          {%- if order.billing_address -%}
          <div class="rk-addr-card">
            <span class="rk-acct-card-label">Billing address</span>
            <p class="rk-addr-lines">{{ order.billing_address.first_name }} {{ order.billing_address.last_name }}<br>
            {{ order.billing_address.address_1 }}{% if order.billing_address.address_2 != blank %}, {{ order.billing_address.address_2 }}{% endif %}<br>
            {{ order.billing_address.city }}{% if order.billing_address.province != blank %}, {{ order.billing_address.province }}{% endif %} {{ order.billing_address.postal_code }}<br>
            {{ order.billing_address.country_code }}</p>
          </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>
