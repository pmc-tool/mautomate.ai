{%- assign href = routes.root_url | append: '/products/' | append: product.handle -%}
{%- assign main = product.featured_image.url | default: product.featured_image -%}
{%- if main == blank and product.images -%}{%- assign main = product.images[0].url -%}{%- endif -%}
{%- if main == blank -%}{%- assign main = '/rokon/img/product/product1.webp' -%}{%- endif -%}
{%- assign hover = main -%}
{%- if product.images and product.images.size > 1 -%}{%- assign hover = product.images[1].url -%}{%- endif -%}
{%- assign on_sale = false -%}
{%- if product.compare_at_price and product.compare_at_price > product.price -%}{%- assign on_sale = true -%}{%- endif -%}
<article class="rk-card">
  <div class="rk-card-media">
    {%- if on_sale -%}<span class="rk-card-badge">Sale</span>{%- endif -%}
    <a href="{{ href }}" class="rk-card-img">
      <img class="rk-card-img-main" src="{{ main }}" alt="{{ product.title }}" loading="lazy">
      {%- if hover != main -%}<img class="rk-card-img-hover" src="{{ hover }}" alt="{{ product.title }}" loading="lazy">{%- endif -%}
    </a>
  </div>
  <div class="rk-card-body">
    <h3 class="rk-card-title"><a href="{{ href }}">{{ product.title }}</a></h3>
    <div class="rk-card-price">
      {%- if on_sale -%}<span class="rk-price-now">{{ product.price | money }}</span><span class="rk-price-div"></span><span class="rk-price-old">{{ product.compare_at_price | money }}</span>
      {%- else -%}<span class="rk-price-now">{{ product.price | money }}</span>{%- endif -%}
    </div>
    <a class="rk-btn rk-btn-dark rk-card-btn" href="{{ href }}">Add To Cart</a>
  </div>
</article>
