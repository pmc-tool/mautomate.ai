{%- assign cols = section.settings.columns -%}
{%- if cols and cols.size > 0 -%}
<section class="rk-section rk-composed">
  <div class="rk-container">
    <div class="rk-composed-row" data-cols="{{ cols.size }}">
      {%- for col in cols -%}
      <div class="rk-composed-col">
        {%- for w in col.widgets -%}
        <div class="rk-composed-widget">
          {%- if w.image != blank -%}<img class="rk-composed-img" src="{{ w.image }}" alt="{{ w.title | default: '' }}">{%- endif -%}
          {%- if w.eyebrow != blank -%}<span class="rk-eyebrow">{{ w.eyebrow }}</span>{%- endif -%}
          {%- if w.title != blank -%}<h3 class="rk-composed-title">{{ w.title }}</h3>{%- endif -%}
          {%- if w.html != blank -%}<div class="rk-richtext-inner">{% render 'resolve-tokens', text: w.html, shop: shop %}</div>{%- endif -%}
          {%- if w.body != blank -%}<p class="rk-composed-body">{{ w.body }}</p>{%- endif -%}
          {%- if w.cta.href != blank -%}<a class="rk-link" href="{{ w.cta.href }}">{{ w.cta.label | default: 'Learn more' }}</a>{%- endif -%}
        </div>
        {%- endfor -%}
      </div>
      {%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
