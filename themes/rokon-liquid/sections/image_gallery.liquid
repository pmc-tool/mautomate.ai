{%- assign s = section.settings -%}
{%- assign images = s.images -%}
{%- if images and images.size > 0 -%}
<section class="rk-section rk-gallery{% if section.in_column %} rk-in-column{% endif %}">
  <div class="rk-container">
    {%- if s.title != blank -%}<div class="rk-shead rk-shead-center"><h2 class="rk-title rk-title-accent" data-el="heading">{{ s.title }}</h2></div>{%- endif -%}
    <div class="rk-gallery-grid">
      {%- for item in images -%}
        {%- assign src = item.image | default: item -%}
        {%- if src != blank -%}
        <figure class="rk-gallery-cell" data-el-item="items:{{ forloop.index0 }}" data-el="item"><img src="{{ src }}" alt="{{ item.alt | default: 'Gallery image' }}">{% if item.caption != blank %}<figcaption data-el="caption">{{ item.caption }}</figcaption>{% endif %}</figure>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
