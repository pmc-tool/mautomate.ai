{%- assign href = routes.root_url | append: '/products/' | append: product.handle -%}
{%- assign main = product.featured_image.url | default: product.featured_image -%}
{%- if main == blank and product.images -%}{%- assign main = product.images[0].url -%}{%- endif -%}
{%- if main == blank -%}{%- assign main = '/learts/assets/images/product/s328/product-1.webp' -%}{%- endif -%}
{%- assign hover = main -%}
{%- if product.images and product.images.size > 1 -%}{%- assign hover = product.images[1].url -%}{%- endif -%}
{%- assign on_sale = false -%}
{%- if product.compare_at_price and product.compare_at_price > product.price -%}{%- assign on_sale = true -%}{%- endif -%}
<article class="lz-card">
  <div class="lz-card-media">
    {%- if on_sale -%}<span class="lz-card-badge">Sale</span>{%- endif -%}
    <a href="{{ href }}" class="lz-card-img">
      <img class="lz-card-img-main" src="{{ main }}" alt="{{ product.title }}" loading="lazy">
      {%- if hover != main -%}<img class="lz-card-img-hover" src="{{ hover }}" alt="{{ product.title }}" loading="lazy">{%- endif -%}
    </a>
  </div>
  <div class="lz-card-body">
    <h3 class="lz-card-title"><a href="{{ href }}">{{ product.title }}</a></h3>
    <div class="lz-card-price">
      {%- if on_sale -%}<span class="lz-price-old">{{ product.compare_at_price | money }}</span> <span class="lz-price-now">{{ product.price | money }}</span>
      {%- else -%}<span class="lz-price-now">{{ product.price | money }}</span>{%- endif -%}
    </div>
  </div>
</article>
