<nav class="lz-crumbs"><div class="lz-container">
  <a href="{{ routes.root_url }}">Home</a><span>/</span><a href="{{ routes.account_url }}">My account</a><span>/</span><span class="lz-current">Addresses</span>
</div></nav>
<section class="lz-section lz-account">
  <div class="lz-container">
    <div class="lz-shead"><h1 class="lz-title">Addresses</h1></div>
    <div class="lz-acct-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'addresses' %}
      <div class="lz-acct-main">
        <div class="lz-acct-toolbar">
          <button type="button" class="lz-btn" data-address-new>Add address</button>
        </div>

        {%- if customer.addresses and customer.addresses.size > 0 -%}
        <div class="lz-addr-grid">
          {%- for address in customer.addresses -%}
          <div class="lz-addr-card">
            <p class="lz-addr-name">{{ address.first_name }} {{ address.last_name }}
              {%- if address.is_default_shipping %} <span class="lz-badge">Default shipping</span>{% endif -%}
              {%- if address.is_default_billing %} <span class="lz-badge">Default billing</span>{% endif -%}
            </p>
            <p class="lz-addr-lines">{{ address.address_1 }}{% if address.address_2 != blank %}, {{ address.address_2 }}{% endif %}<br>
            {{ address.city }}{% if address.province != blank %}, {{ address.province }}{% endif %} {{ address.postal_code }}<br>
            {{ address.country_code }}{% if address.phone != blank %} &middot; {{ address.phone }}{% endif %}</p>
            <div class="lz-addr-actions">
              <button type="button" class="lz-link" data-address-edit
                data-a-address_id="{{ address.id }}"
                data-a-first_name="{{ address.first_name }}"
                data-a-last_name="{{ address.last_name }}"
                data-a-company="{{ address.company }}"
                data-a-address_1="{{ address.address_1 }}"
                data-a-address_2="{{ address.address_2 }}"
                data-a-city="{{ address.city }}"
                data-a-postal_code="{{ address.postal_code }}"
                data-a-province="{{ address.province }}"
                data-a-country_code="{{ address.country_code | downcase }}"
                data-a-phone="{{ address.phone }}">Edit</button>
              <button type="button" class="lz-link lz-link-danger" data-address-delete="{{ address.id }}">Delete</button>
            </div>
          </div>
          {%- endfor -%}
        </div>
        {%- else -%}
        <div class="lz-acct-empty"><p>No saved addresses yet.</p></div>
        {%- endif -%}

        <form class="lz-addr-form" data-account-form="address" data-success="Address saved." hidden>
          <h2 class="lz-acct-h2">Address details</h2>
          <input type="hidden" name="address_id" value="">
          <div class="lz-field-row">
            <label class="lz-field"><span>First name</span><input type="text" name="first_name" required></label>
            <label class="lz-field"><span>Last name</span><input type="text" name="last_name" required></label>
          </div>
          <label class="lz-field"><span>Company <em>(optional)</em></span><input type="text" name="company"></label>
          <label class="lz-field"><span>Address</span><input type="text" name="address_1" required></label>
          <label class="lz-field"><span>Apartment, suite, etc. <em>(optional)</em></span><input type="text" name="address_2"></label>
          <div class="lz-field-row">
            <label class="lz-field"><span>City</span><input type="text" name="city" required></label>
            <label class="lz-field"><span>Postal code</span><input type="text" name="postal_code" required></label>
          </div>
          <div class="lz-field-row">
            <label class="lz-field"><span>Province / state <em>(optional)</em></span><input type="text" name="province"></label>
            <label class="lz-field"><span>Country</span>
              <select class="lz-select" name="country_code" required>
                {%- for country in countries -%}
                <option value="{{ country.code }}">{{ country.name }}</option>
                {%- endfor -%}
              </select>
            </label>
          </div>
          <label class="lz-field"><span>Phone <em>(optional)</em></span><input type="tel" name="phone"></label>
          <div class="lz-field-row">
            <label class="lz-check"><input type="checkbox" name="default_shipping"><span>Default shipping address</span></label>
            <label class="lz-check"><input type="checkbox" name="default_billing"><span>Default billing address</span></label>
          </div>
          <p class="lz-form-msg" data-form-msg hidden></p>
          <button type="submit" class="lz-btn">Save address</button>
        </form>
      </div>
    </div>
  </div>
</section>
