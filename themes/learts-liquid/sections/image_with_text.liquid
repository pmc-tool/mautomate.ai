{%- assign s = section.settings -%}
{%- assign nl = '
' -%}
{%- if s.image != blank or s.title != blank -%}
<section class="lz-section lz-imgtext{% if s.image_side == 'right' %} lz-imgtext-right{% endif %}">
  <div class="lz-container">
    <div class="lz-imgtext-grid">
      <div class="lz-imgtext-media">{% if s.image %}<img src="{{ s.image }}" alt="{{ s.title }}">{% endif %}</div>
      <div class="lz-imgtext-body">
        {%- if s.eyebrow != blank -%}<span class="lz-eyebrow">{{ s.eyebrow }}</span>{%- endif -%}
        {%- if s.title != blank -%}<h2 class="lz-title">{%- assign lines = s.title | split: nl -%}{%- for line in lines -%}{{ line }}{%- unless forloop.last -%}<br>{%- endunless -%}{%- endfor -%}</h2>{%- endif -%}
        {%- if s.body != blank -%}<p class="lz-imgtext-copy">{{ s.body }}</p>{%- endif -%}
        {%- if s.cta.href != blank -%}<a class="lz-btn" href="{{ s.cta.href }}">{{ s.cta.label | default: 'Learn more' }}</a>{%- endif -%}
      </div>
    </div>
  </div>
</section>
{%- endif -%}
