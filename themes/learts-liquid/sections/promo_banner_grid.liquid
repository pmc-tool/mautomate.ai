{%- assign s = section.settings -%}
<section class="lz-section lz-promo">
  <div class="lz-container">
    <div class="lz-promo-grid">
      {%- if s.intro -%}
      <div class="lz-promo-intro">
        <h2 class="lz-promo-intro-title">{{ s.intro.title }}</h2>
        {%- if s.intro.body != blank -%}<p class="lz-promo-intro-body">{{ s.intro.body }}</p>{%- endif -%}
        {%- if s.intro.href != blank -%}<a class="lz-link" href="{{ s.intro.href }}">{{ s.intro.link_label | default: 'Learn more' }}</a>{%- endif -%}
      </div>
      {%- endif -%}

      {%- if s.sale -%}
      <a class="lz-promo-sale" href="{{ s.sale.href | default: '/store' }}">
        {%- if s.sale.image %}<img src="{{ s.sale.image }}" alt="{{ s.sale.title }}">{% endif -%}
        <div class="lz-promo-sale-body">
          {%- if s.sale.special_title != blank -%}<span class="lz-eyebrow lz-eyebrow-light">{{ s.sale.special_title }}</span>{%- endif -%}
          <h3 class="lz-promo-sale-title">{{ s.sale.title }}</h3>
          {%- if s.sale.href != blank -%}<span class="lz-link lz-link-light">{{ s.sale.link_label | default: 'Shop now' }}</span>{%- endif -%}
        </div>
      </a>
      {%- endif -%}

      {%- for cat in s.categories -%}{%- unless cat.wide -%}
      <a class="lz-promo-cat" href="{{ cat.href | default: '/store' }}">
        <div class="lz-promo-cat-img">{% if cat.image %}<img src="{{ cat.image }}" alt="{{ cat.title }}">{% endif %}</div>
        <div class="lz-promo-cat-cap"><h3>{{ cat.title }}</h3>{% if cat.count_label != blank %}<span>{{ cat.count_label }}</span>{% endif %}</div>
      </a>
      {%- endunless -%}{%- endfor -%}

      {%- if s.instagram -%}
      <a class="lz-promo-insta" href="{{ s.instagram.href | default: '#' }}">
        {%- if s.instagram.image %}<img src="{{ s.instagram.image }}" alt="{{ s.instagram.handle }}">{% endif -%}
        <div class="lz-promo-insta-body">
          <i class="fab fa-instagram"></i>
          {%- if s.instagram.sub_title != blank -%}<span>{{ s.instagram.sub_title }}</span>{%- endif -%}
          <h3>{{ s.instagram.handle }}</h3>
        </div>
      </a>
      {%- endif -%}

      {%- for cat in s.categories -%}{%- if cat.wide -%}
      <a class="lz-promo-cat lz-promo-cat-wide" href="{{ cat.href | default: '/store' }}">
        <div class="lz-promo-cat-img">{% if cat.image %}<img src="{{ cat.image }}" alt="{{ cat.title }}">{% endif %}</div>
        <div class="lz-promo-cat-cap"><h3>{{ cat.title }}</h3>{% if cat.count_label != blank %}<span>{{ cat.count_label }}</span>{% endif %}</div>
      </a>
      {%- endif -%}{%- endfor -%}
    </div>
  </div>
</section>
