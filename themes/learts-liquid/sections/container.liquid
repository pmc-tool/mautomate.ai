{%- assign cols = section.settings.columns -%}
{%- if cols and cols.size > 0 -%}
<section class="lz-section lz-composed">
  <div class="lz-container">
    <div class="lz-composed-row" data-cols="{{ cols.size }}">
      {%- for col in cols -%}
      <div class="lz-composed-col">
        {%- for w in col.widgets -%}
        <div class="lz-composed-widget">
          {%- if w.image != blank -%}<img class="lz-composed-img" src="{{ w.image }}" alt="{{ w.title | default: '' }}">{%- endif -%}
          {%- if w.eyebrow != blank -%}<span class="lz-eyebrow">{{ w.eyebrow }}</span>{%- endif -%}
          {%- if w.title != blank -%}<h3 class="lz-composed-title">{{ w.title }}</h3>{%- endif -%}
          {%- if w.html != blank -%}<div class="lz-richtext-inner">{% render 'resolve-tokens', text: w.html, shop: shop %}</div>{%- endif -%}
          {%- if w.body != blank -%}<p class="lz-composed-body">{{ w.body }}</p>{%- endif -%}
          {%- if w.cta.href != blank -%}<a class="lz-link" href="{{ w.cta.href }}">{{ w.cta.label | default: 'Learn more' }}</a>{%- endif -%}
        </div>
        {%- endfor -%}
      </div>
      {%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
