{%- assign slides = section.settings.slides -%}
{%- assign nl = '
' -%}
{%- if slides and slides.size > 0 -%}
<section class="cg-hero{% if section.in_column %} cg-in-column{% endif %}" data-hero data-autoplay="{{ section.settings.autoplay_ms | default: 6000 }}">
  <div class="cg-hero-slides">
    {%- for slide in slides -%}
    <div class="cg-hero-slide{% if forloop.first %} is-active{% endif %}" data-el-item="slides:{{ forloop.index0 }}" data-el="image"{% if slide.image != blank %} style="background-image:url('{{ slide.image }}')"{% endif %}>
      <div class="cg-container cg-hero-inner">
        <div class="cg-hero-content" data-el="content">
          {%- if slide.subtitle != blank -%}<span class="cg-eyebrow cg-eyebrow-light" data-el="kicker">{{ slide.subtitle }}</span>{%- endif -%}
          <h1 class="cg-hero-title" data-el="title">{%- assign lines = slide.title | split: nl -%}{%- for line in lines -%}{{ line }}{%- unless forloop.last -%}<br>{%- endunless -%}{%- endfor -%}</h1>
          {%- if slide.cta.href != blank -%}<a class="cg-btn cg-btn-light" href="{{ slide.cta.href }}" data-el="button">{{ slide.cta.label | default: 'Shop now' }}</a>{%- endif -%}
        </div>
      </div>
    </div>
    {%- endfor -%}
  </div>
  {%- if slides.size > 1 -%}
  {%- unless section.in_column -%}<button class="cg-hero-arrow cg-hero-prev" data-hero-prev aria-label="Previous">&#8249;</button>
  <button class="cg-hero-arrow cg-hero-next" data-hero-next aria-label="Next">&#8250;</button>
  {% endunless %}<div class="cg-hero-dots" data-hero-dots>
    {%- for slide in slides -%}<button class="cg-hero-dot{% if forloop.first %} is-active{% endif %}" data-hero-dot="{{ forloop.index0 }}" aria-label="Slide {{ forloop.index }}"></button>{%- endfor -%}
  </div>
  {%- endif -%}
</section>
{%- endif -%}
