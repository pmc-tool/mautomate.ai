<div class="cg-pagehead">
  <div class="cg-container">
    <h1 class="cg-pagehead-title">Order {{ order.name }}</h1>
    <nav class="cg-pagehead-crumbs"><a href="{{ routes.root_url }}">Home</a><span>/</span><a href="{{ routes.account_url }}/orders">Orders</a><span>/</span><span class="cg-current">{{ order.name }}</span></nav>
  </div>
</div>
<section class="cg-section cg-account">
  <div class="cg-container">
    <div class="cg-acct-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'orders' %}
      <div class="cg-acct-main">
        <p class="cg-order-meta">Placed {{ order.created_at | date: "%B %d, %Y" }} &middot; <span class="cg-badge">{{ order.fulfillment_status | replace: "_", " " | capitalize }}</span> <span class="cg-badge">{{ order.payment_status | replace: "_", " " | capitalize }}</span></p>

        <div class="cg-order-lines">
          {%- for item in order.items -%}
          <div class="cg-order-line">
            {%- if item.image %}<img src="{{ item.image }}" alt="{{ item.title }}">{% endif -%}
            <div class="cg-order-line-info">
              <p class="cg-order-line-title">{{ item.title }}</p>
              {%- if item.variant_title != blank and item.variant_title != "Default Variant" -%}
              <span class="cg-order-line-variant">{{ item.variant_title }}</span>
              {%- endif -%}
              <span class="cg-order-line-qty">Qty {{ item.quantity }} &times; {{ item.unit_price | money }}</span>
            </div>
            <span class="cg-order-line-total">{{ item.total | money }}</span>
          </div>
          {%- endfor -%}
        </div>

        <div class="cg-order-summary">
          <div class="cg-osum-row"><span>Subtotal</span><span>{{ order.subtotal | money }}</span></div>
          {%- if order.discount_total > 0 -%}
          <div class="cg-osum-row cg-osum-discount"><span>Discount</span><span>&minus;{{ order.discount_total | money }}</span></div>
          {%- endif -%}
          <div class="cg-osum-row"><span>Shipping</span><span>{{ order.shipping_total | money }}</span></div>
          {%- if order.tax_total > 0 -%}
          <div class="cg-osum-row"><span>Tax</span><span>{{ order.tax_total | money }}</span></div>
          {%- endif -%}
          <div class="cg-osum-row cg-osum-total"><span>Total</span><span>{{ order.total | money }}</span></div>
        </div>

        <div class="cg-order-addresses">
          {%- if order.shipping_address -%}
          <div class="cg-addr-card">
            <span class="cg-acct-card-label">Shipping address</span>
            <p>{{ order.shipping_address.first_name }} {{ order.shipping_address.last_name }}<br>
            {{ order.shipping_address.address_1 }}{% if order.shipping_address.address_2 != blank %}, {{ order.shipping_address.address_2 }}{% endif %}<br>
            {{ order.shipping_address.city }}{% if order.shipping_address.province != blank %}, {{ order.shipping_address.province }}{% endif %} {{ order.shipping_address.postal_code }}<br>
            {{ order.shipping_address.country_code }}</p>
          </div>
          {%- endif -%}
          {%- if order.billing_address -%}
          <div class="cg-addr-card">
            <span class="cg-acct-card-label">Billing address</span>
            <p>{{ order.billing_address.first_name }} {{ order.billing_address.last_name }}<br>
            {{ order.billing_address.address_1 }}{% if order.billing_address.address_2 != blank %}, {{ order.billing_address.address_2 }}{% endif %}<br>
            {{ order.billing_address.city }}{% if order.billing_address.province != blank %}, {{ order.billing_address.province }}{% endif %} {{ order.billing_address.postal_code }}<br>
            {{ order.billing_address.country_code }}</p>
          </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
</section>
