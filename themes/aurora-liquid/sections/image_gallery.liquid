{%- assign s = section.settings -%}
{%- assign images = s.images -%}
{%- if images and images.size > 0 -%}
<section class="au-section au-gallery{% if section.in_column %} au-in-column{% endif %}">
  <div class="au-container">
    {%- if s.title != blank -%}<div class="au-shead au-shead-center"><h2 class="au-title" data-el="heading">{{ s.title }}</h2></div>{%- endif -%}
    <div class="au-gallery-grid" data-el="grid">
      {%- for item in images -%}
        {%- assign src = item.image | default: item -%}
        {%- if src != blank -%}
        <figure class="au-gallery-cell au-card" data-el-item="items:{{ forloop.index0 }}" data-el="item"><img src="{{ src }}" alt="{{ item.alt | default: 'Gallery image' }}">{% if item.caption != blank %}<figcaption data-el="caption">{{ item.caption }}</figcaption>{% endif %}</figure>
        {%- endif -%}
      {%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
