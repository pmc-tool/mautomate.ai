{%- assign p = product -%}
<nav class="au-crumbs"><div class="au-container">
  <a href="{{ routes.root_url }}">Home</a><span class="au-crumb-sep">/</span><a href="{{ routes.collections_url }}">Shop</a><span class="au-crumb-sep">/</span><span class="au-crumb-current">{{ p.title }}</span>
</div></nav>

<section class="au-section au-pdp">
  <div class="au-container">
    <div class="au-pdp-grid">
      <div class="au-pdp-gallery">
        {%- assign main = p.featured_image.url | default: p.featured_image -%}
        {%- if main == blank -%}{%- assign main = '/learts/assets/images/product/s328/product-1.webp' -%}{%- endif -%}
        <div class="au-pdp-main au-card"><img id="pdp-main-img" src="{{ main }}" alt="{{ p.title }}"></div>
        {%- if p.images and p.images.size > 1 -%}
        <div class="au-pdp-thumbs" data-pdp-thumbs>
          {%- for image in p.images -%}<button class="au-pdp-thumb{% if forloop.first %} is-active{% endif %}" data-full="{{ image.url }}"><img src="{{ image.url }}" alt="{{ image.alt | default: p.title }}"></button>{%- endfor -%}
        </div>
        {%- endif -%}
      </div>

      <div class="au-pdp-info">
        <h1 class="au-pdp-title">{{ p.title }}</h1>
        <div class="au-pdp-price">
          <span class="au-price-now">{{ p.price | money }}</span>
          {%- if p.compare_at_price and p.compare_at_price > p.price -%} <span class="au-price-old">{{ p.compare_at_price | money }}</span>{%- endif -%}
        </div>
        {%- if p.available -%}<span class="au-stock in">In stock</span>{%- else -%}<span class="au-stock out">Out of stock</span>{%- endif -%}
        {%- if p.description != blank -%}<div class="au-pdp-desc"><p>{{ p.description }}</p></div>{%- endif -%}

        {%- for option in p.options -%}
        <div class="au-pdp-opt"><span class="au-pdp-opt-name">{{ option.name }}</span><div class="au-pdp-opt-vals">{%- for value in option.values -%}<span class="au-chip">{{ value }}</span>{%- endfor -%}</div></div>
        {%- endfor -%}

        <form class="au-pdp-cart" data-add-to-cart>
          {%- if p.variants and p.variants.size > 1 -%}
          <div class="au-pdp-opt"><span class="au-pdp-opt-name">Variant</span>
            <select class="au-select" data-variant-select>
              {%- for v in p.variants -%}<option value="{{ v.id }}" {% unless v.available %}disabled{% endunless %}>{{ v.title }} — {{ v.price | money }}{% unless v.available %} (sold out){% endunless %}</option>{%- endfor -%}
            </select>
          </div>
          {%- elsif p.variants and p.variants.size == 1 -%}
          <input type="hidden" data-variant-select value="{{ p.variants[0].id }}">
          {%- endif -%}
          <div class="au-pdp-buy">
            <input type="number" class="au-qty" data-qty value="1" min="1" aria-label="Quantity">
            <button type="submit" class="au-btn au-btn-lg" data-add-btn {% unless p.available %}disabled{% endunless %}>{% if p.available %}Add to cart{% else %}Sold out{% endif %}</button>
          </div>
          <p class="au-pdp-msg" data-cart-msg hidden></p>
        </form>
      </div>
    </div>
  </div>
</section>
