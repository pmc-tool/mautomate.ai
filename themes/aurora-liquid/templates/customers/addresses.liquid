<nav class="au-crumbs"><div class="au-container">
  <a href="{{ routes.root_url }}">Home</a><span class="au-crumb-sep">/</span><a href="{{ routes.account_url }}">My account</a><span class="au-crumb-sep">/</span><span class="au-crumb-current">Addresses</span>
</div></nav>

<section class="au-section au-account">
  <div class="au-container">
    <header class="au-listing-head">
      <span class="au-eyebrow">Your account</span>
      <h1 class="au-listing-title">Addresses</h1>
    </header>
    <div class="au-acct-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'addresses' %}
      <div class="au-acct-main">
        <div class="au-acct-toolbar">
          <button type="button" class="au-btn" data-address-new>Add address</button>
        </div>

        {%- if customer.addresses and customer.addresses.size > 0 -%}
        <div class="au-addr-grid">
          {%- for address in customer.addresses -%}
          <div class="au-addr-card au-card-flat">
            <p class="au-addr-name">{{ address.first_name }} {{ address.last_name }}
              {%- if address.is_default_shipping %} <span class="au-badge">Default shipping</span>{% endif -%}
              {%- if address.is_default_billing %} <span class="au-badge">Default billing</span>{% endif -%}
            </p>
            <p class="au-addr-lines">{{ address.address_1 }}{% if address.address_2 != blank %}, {{ address.address_2 }}{% endif %}<br>
            {{ address.city }}{% if address.province != blank %}, {{ address.province }}{% endif %} {{ address.postal_code }}<br>
            {{ address.country_code }}{% if address.phone != blank %} &middot; {{ address.phone }}{% endif %}</p>
            <div class="au-addr-actions">
              <button type="button" class="au-textlink" data-address-edit
                data-a-address_id="{{ address.id }}"
                data-a-first_name="{{ address.first_name }}"
                data-a-last_name="{{ address.last_name }}"
                data-a-company="{{ address.company }}"
                data-a-address_1="{{ address.address_1 }}"
                data-a-address_2="{{ address.address_2 }}"
                data-a-city="{{ address.city }}"
                data-a-postal_code="{{ address.postal_code }}"
                data-a-province="{{ address.province }}"
                data-a-country_code="{{ address.country_code | downcase }}"
                data-a-phone="{{ address.phone }}">Edit</button>
              <button type="button" class="au-textlink au-link-danger" data-address-delete="{{ address.id }}">Delete</button>
            </div>
          </div>
          {%- endfor -%}
        </div>
        {%- else -%}
        <div class="au-acct-empty au-card-flat"><p>No saved addresses yet.</p></div>
        {%- endif -%}

        <form class="au-addr-form au-card-flat" data-account-form="address" data-success="Address saved." hidden>
          <h2 class="au-acct-h2">Address details</h2>
          <input type="hidden" name="address_id" value="">
          <div class="au-field-row">
            <label class="au-field"><span>First name</span><input type="text" name="first_name" required></label>
            <label class="au-field"><span>Last name</span><input type="text" name="last_name" required></label>
          </div>
          <label class="au-field"><span>Company <em>(optional)</em></span><input type="text" name="company"></label>
          <label class="au-field"><span>Address</span><input type="text" name="address_1" required></label>
          <label class="au-field"><span>Apartment, suite, etc. <em>(optional)</em></span><input type="text" name="address_2"></label>
          <div class="au-field-row">
            <label class="au-field"><span>City</span><input type="text" name="city" required></label>
            <label class="au-field"><span>Postal code</span><input type="text" name="postal_code" required></label>
          </div>
          <div class="au-field-row">
            <label class="au-field"><span>Province / state <em>(optional)</em></span><input type="text" name="province"></label>
            <label class="au-field"><span>Country</span>
              <select name="country_code" required>
                {%- for country in countries -%}
                <option value="{{ country.code }}">{{ country.name }}</option>
                {%- endfor -%}
              </select>
            </label>
          </div>
          <label class="au-field"><span>Phone <em>(optional)</em></span><input type="tel" name="phone"></label>
          <div class="au-field-row au-check-row">
            <label class="au-check"><input type="checkbox" name="default_shipping"><span>Default shipping address</span></label>
            <label class="au-check"><input type="checkbox" name="default_billing"><span>Default billing address</span></label>
          </div>
          <p class="au-form-msg" data-form-msg hidden></p>
          <button type="submit" class="au-btn">Save address</button>
        </form>
      </div>
    </div>
  </div>
</section>
