<nav class="ex-crumbs"><div class="ex-container">
  <a href="{{ routes.root_url }}">Home</a><span>/</span><a href="{{ routes.account_url }}">My account</a><span>/</span><span class="ex-current">Addresses</span>
</div></nav>
<section class="ex-section ex-account">
  <div class="ex-container">
    <div class="ex-shead">
      <h1 class="ex-title">Addresses</h1>
      <span class="ex-underline ex-underline-left"><span></span></span>
    </div>
    <div class="ex-acct-grid">
      {% render 'account-nav', routes: routes, customer: customer, active: 'addresses' %}
      <div class="ex-acct-main">
        <div class="ex-acct-toolbar">
          <button type="button" class="ex-btn" data-address-new>Add address</button>
        </div>

        {%- if customer.addresses and customer.addresses.size > 0 -%}
        <div class="ex-addr-grid">
          {%- for address in customer.addresses -%}
          <div class="ex-addr-card">
            <p class="ex-addr-name">{{ address.first_name }} {{ address.last_name }}
              {%- if address.is_default_shipping %} <span class="ex-badge">Default shipping</span>{% endif -%}
              {%- if address.is_default_billing %} <span class="ex-badge">Default billing</span>{% endif -%}
            </p>
            <p class="ex-addr-lines">{{ address.address_1 }}{% if address.address_2 != blank %}, {{ address.address_2 }}{% endif %}<br>
            {{ address.city }}{% if address.province != blank %}, {{ address.province }}{% endif %} {{ address.postal_code }}<br>
            {{ address.country_code }}{% if address.phone != blank %} &middot; {{ address.phone }}{% endif %}</p>
            <div class="ex-addr-actions">
              <button type="button" class="ex-link" data-address-edit
                data-a-address_id="{{ address.id }}"
                data-a-first_name="{{ address.first_name }}"
                data-a-last_name="{{ address.last_name }}"
                data-a-company="{{ address.company }}"
                data-a-address_1="{{ address.address_1 }}"
                data-a-address_2="{{ address.address_2 }}"
                data-a-city="{{ address.city }}"
                data-a-postal_code="{{ address.postal_code }}"
                data-a-province="{{ address.province }}"
                data-a-country_code="{{ address.country_code | downcase }}"
                data-a-phone="{{ address.phone }}">Edit</button>
              <button type="button" class="ex-link ex-link-danger" data-address-delete="{{ address.id }}">Delete</button>
            </div>
          </div>
          {%- endfor -%}
        </div>
        {%- else -%}
        <div class="ex-acct-empty"><p>No saved addresses yet.</p></div>
        {%- endif -%}

        <form class="ex-addr-form" data-account-form="address" data-success="Address saved." hidden>
          <h2 class="ex-acct-h2">Address details</h2>
          <input type="hidden" name="address_id" value="">
          <div class="ex-field-row">
            <label class="ex-field"><span>First name</span><input type="text" name="first_name" required></label>
            <label class="ex-field"><span>Last name</span><input type="text" name="last_name" required></label>
          </div>
          <label class="ex-field"><span>Company <em>(optional)</em></span><input type="text" name="company"></label>
          <label class="ex-field"><span>Address</span><input type="text" name="address_1" required></label>
          <label class="ex-field"><span>Apartment, suite, etc. <em>(optional)</em></span><input type="text" name="address_2"></label>
          <div class="ex-field-row">
            <label class="ex-field"><span>City</span><input type="text" name="city" required></label>
            <label class="ex-field"><span>Postal code</span><input type="text" name="postal_code" required></label>
          </div>
          <div class="ex-field-row">
            <label class="ex-field"><span>Province / state <em>(optional)</em></span><input type="text" name="province"></label>
            <label class="ex-field"><span>Country</span>
              <select class="ex-select ex-select-field" name="country_code" required>
                {%- for country in countries -%}
                <option value="{{ country.code }}">{{ country.name }}</option>
                {%- endfor -%}
              </select>
            </label>
          </div>
          <label class="ex-field"><span>Phone <em>(optional)</em></span><input type="tel" name="phone"></label>
          <div class="ex-field-row">
            <label class="ex-check"><input type="checkbox" name="default_shipping"><span>Default shipping address</span></label>
            <label class="ex-check"><input type="checkbox" name="default_billing"><span>Default billing address</span></label>
          </div>
          <p class="ex-form-msg" data-form-msg hidden></p>
          <button type="submit" class="ex-btn">Save address</button>
        </form>
      </div>
    </div>
  </div>
</section>
