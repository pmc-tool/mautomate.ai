{%- assign s = section.settings -%}
{%- assign brands = s.brands -%}
<section class="ex-section ex-brands{% if section.in_column %} ex-in-column{% endif %}">
  <div class="ex-container">
    {%- if s.title != blank -%}
    <div class="ex-shead ex-shead-center">
      <h2 class="ex-title ex-title-sm" data-el="title">{{ s.title }}</h2>
      <span class="ex-underline"><span></span></span>
    </div>
    {%- endif -%}
    {%- if brands and brands.size > 0 -%}
    <div class="ex-brand-row">
      {%- for brand in brands -%}<a class="ex-brand" href="{{ brand.href | default: '#' }}" data-el-item="brands:{{ forloop.index0 }}" data-el="logo">{% if brand.image != blank %}<img src="{{ brand.image }}" alt="Brand">{% endif %}</a>{%- endfor -%}
    </div>
    {%- endif -%}
  </div>
</section>
