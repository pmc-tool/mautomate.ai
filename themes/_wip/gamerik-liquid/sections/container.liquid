{%- assign cols = section.settings.columns -%}
{%- if cols and cols.size > 0 -%}
<section class="gm-section gm-composed">
  <div class="gm-container">
    <div class="gm-composed-row" data-cols="{{ cols.size }}">
      {%- for col in cols -%}
      <div class="gm-composed-col">
        {%- for w in col.widgets -%}
        <div class="gm-composed-widget">
          {%- if w.image != blank -%}<img class="gm-composed-img" src="{{ w.image }}" alt="{{ w.title | default: '' }}">{%- endif -%}
          {%- if w.eyebrow != blank -%}<span class="gm-eyebrow">{{ w.eyebrow }}</span>{%- endif -%}
          {%- if w.title != blank -%}<h3 class="gm-composed-title">{{ w.title }}</h3>{%- endif -%}
          {%- if w.html != blank -%}<div class="gm-richtext-inner">{% render 'resolve-tokens', text: w.html, shop: shop %}</div>{%- endif -%}
          {%- if w.body != blank -%}<p class="gm-composed-body">{{ w.body }}</p>{%- endif -%}
          {%- if w.cta.href != blank -%}<a class="gm-link" href="{{ w.cta.href }}">{{ w.cta.label | default: 'Learn more' }}</a>{%- endif -%}
        </div>
        {%- endfor -%}
      </div>
      {%- endfor -%}
    </div>
  </div>
</section>
{%- endif -%}
