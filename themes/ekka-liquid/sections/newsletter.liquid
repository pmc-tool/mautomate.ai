{%- assign s = section.settings -%}
{%- if s.title != blank -%}
<section class="ek-section ek-newsletter">
  <div class="ek-container">
    <div class="ek-newsletter-inner">
      <h2 class="ek-newsletter-title" data-el="heading">{{ s.title }}</h2>
      {%- if s.subtitle != blank -%}<p class="ek-newsletter-sub" data-el="text">{{ s.subtitle }}</p>{%- endif -%}
      <form class="ek-subscribe ek-subscribe-lg" data-newsletter data-el="form">
        <input type="email" placeholder="{{ s.placeholder | default: 'Enter your email here...' }}" aria-label="Email address" data-el="input" required>
        <button type="submit" data-el="button" aria-label="{{ s.button | default: 'Subscribe' }}" title="{{ s.button | default: 'Subscribe' }}"><i class="far fa-paper-plane"></i></button>
      </form>
      {%- if s.provider_note != blank -%}<p class="ek-newsletter-note">{{ s.provider_note }}</p>{%- endif -%}
    </div>
  </div>
</section>
{%- endif -%}
