diff --git a/node_modules/@medusajs/event-bus-redis/dist/services/event-bus-redis.js b/node_modules/@medusajs/event-bus-redis/dist/services/event-bus-redis.js
index 66aaac7..1f6a461 100644
--- a/node_modules/@medusajs/event-bus-redis/dist/services/event-bus-redis.js
+++ b/node_modules/@medusajs/event-bus-redis/dist/services/event-bus-redis.js
@@ -13,7 +13,11 @@ class RedisEventBusService extends utils_1.AbstractEventBusModuleService {
         super(...arguments);
         this.__hooks = {
             onApplicationStart: async () => {
-                await this.bullWorker_?.run();
+                // NOTE: BullMQ worker.run() returns a promise that only resolves when the worker closes.
+                // Awaiting it here would block Medusa server startup forever, so we start it without awaiting.
+                this.bullWorker_?.run().catch((err) => {
+                    this.logger_?.error(`Redis event bus worker error: ${err.message}`);
+                });
             },
             onApplicationShutdown: async () => {
                 await this.queue_.close();
